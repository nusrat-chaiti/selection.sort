# selection sort

selection sort is simple algorithm for sorting.Initially, the sorted part is empty and the unsorted part is entire list.the smallest element is selected from the unsorted array and swapped with the left most element, and the element becomes the part of the sorted array.
